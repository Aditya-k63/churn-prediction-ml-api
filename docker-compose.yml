version: "3.8"

services:
  app:
    build: .
    image: adsharma14/churn-prediction-api:latest
    container_name: churn-app
    ports:
      - "8000:8000"
      - "8501:8501"
    volumes:
      - .:/app
    restart: always